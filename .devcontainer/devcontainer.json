{
  "name": "Minecraft Java Server",
  "image": "mcr.microsoft.com/devcontainers/java:25-bookworm",
  "customizations": {
    "vscode": {
      "extensions": []
    }
  },
  "postCreateCommand": "sudo apt-get update -qq && sudo apt-get install -y jq -qq && chmod +x start.sh && echo '[✓] Environment ready'"
}
